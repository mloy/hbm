set (PACKAGE_VERSION @HBM_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @HBM_MAJOR_VERSION@)
  set(PACKAGE_VERSION_COMPATIBLE @HBM_MAJOR_VERSION@) # compatible with any version m.x
  if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @HBM_MINOR_VERSION@)
    set(PACKAGE_VERSION_EXACT 1)    # exact match for version m.n
  endif("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @HBM_MINOR_VERSION@)
endif("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @HBM_MAJOR_VERSION@)

